# Wave 3 작업지시서: P2 기술부채 (24개)

## 개요
- **목표**: 기술부채 제거 → 타입 안전성, 코드 품질, 테스트 커버리지 강화
- **병렬**: 4개 에이전트 동시 실행
- **파일**: ~12개 수정
- **예상 시간**: 40-50분 (병렬)
- **결과**: 타입 에러 0개, 테스트 60%+, 보안 이슈 완전 제거

---

## Agent H: 타입 안전성 강화 (6개 이슈)

**수정 파일**:
```
1. src/lib/b2b/service.ts (where 절, 변수)
2. src/lib/b2b/service.ts (updateData 객체)
3. src/lib/b2b/service.ts (formatProspect 함수)
4. src/app/api/b2b-landing/[id]/comments/generate/route.ts (Claude 응답)
5. src/app/api/b2b-landing/[id]/stats/route.ts (Redis 캐시)
6. src/app/api/b2b-landing/[id]/comments/route.ts (Redis 캐시)
```

### Issue 1: any 타입 - where 절 (라인 20)
**파일**: `src/lib/b2b/service.ts`
```typescript
// 현재
const where: any = { organizationId, deletedAt: null };

// 수정
type WhereInput = {
  organizationId: string;
  deletedAt: null;
  phone?: { in: string[] };
};

const where: WhereInput = { organizationId, deletedAt: null };
```

### Issue 2: any 타입 - updateData (라인 144)
**파일**: `src/lib/b2b/service.ts`
```typescript
// 현재
const updateData: any = {};
if (name) updateData.name = name;

// 수정
type UpdateData = Partial<{
  name: string;
  email: string;
  company: string;
  // ... 다른 필드
}>;

const updateData: UpdateData = {};
```

### Issue 3: any 파라미터 - formatProspect (라인 194)
**파일**: `src/lib/b2b/service.ts`
```typescript
// 현재
function formatProspect(prospect: any) {
  return { id: prospect.id, name: prospect.name };
}

// 수정
interface ProspectFormatInput {
  id: string;
  name: string;
  phone: string;
  email?: string;
}

function formatProspect(prospect: ProspectFormatInput) {
  return { id: prospect.id, name: prospect.name };
}
```

### Issue 4: 타입 단언 - Claude 응답 (라인 101)
**파일**: `src/app/api/b2b-landing/[id]/comments/generate/route.ts`
```typescript
// 현재
const raw = (message.content[0] as { type: string; text: string }).text.trim();

// 수정
interface ClaudeContent {
  type: 'text' | 'image';
  text?: string;
}

const content = message.content[0];
if (content.type !== 'text' || !content.text) {
  throw new ServerError('Claude 응답 형식 오류');
}
const raw = content.text.trim();
```

### Issue 5: any 제네릭 - Redis 캐시 stats (라인 40)
**파일**: `src/app/api/b2b-landing/[id]/stats/route.ts`
```typescript
// 현재
const cachedStats = await getCache<any>(cacheKey);

// 수정
interface B2BStats {
  viewCount: number;
  registered: number;
  emailSent: number;
  funnelEntered: number;
  purchased: number;
  rates: { view2reg: number; reg2email: number; email2funnel: number };
}

const cachedStats = await getCache<B2BStats>(cacheKey);
```

### Issue 6: any 제네릭 - Redis 캐시 comments (라인 44)
**파일**: `src/app/api/b2b-landing/[id]/comments/route.ts`
```typescript
// 현재
const cachedComments = await getCache<any>(cacheKey);

// 수정
interface B2BComment {
  id: string;
  authorName: string;
  content: string;
  isAutoGenerated: boolean;
  createdAt: string;
}

const cachedComments = await getCache<B2BComment[]>(cacheKey);
```

---

## Agent I: 타입 + 초기 코드 스멜 (6개 이슈)

**수정 파일**:
```
1. src/lib/b2b/service.ts (formConfig, 제네릭)
2. src/app/(dashboard)/b2b-editor/[id]/page.tsx (타입, MgrComment)
3. src/app/api/b2b-landing/[id]/comments/route.ts (null 체크, 변수 shadowing)
4. src/app/api/b2b-landing/[id]/stats/route.ts (startTime 중복)
5. src/app/b2b/p/[partnerId]/B2BLandingClient.tsx (타입 단언)
6. src/app/(dashboard)/b2b-editor/new/page.tsx (formConfig: z.any)
```

### Issue 7: formConfig 타입 (schema zod)
**파일**: `src/lib/b2b/service.ts`
```typescript
// 현재
z.any().optional(), // formConfig

// 수정
z.record(z.string(), z.unknown()).optional(), // formConfig: Record<string, unknown>
```

### Issue 8: MgrComment 중복 타입 정의 (라인 76)
**파일**: `src/app/(dashboard)/b2b-editor/[id]/page.tsx`
```typescript
// 현재 (로컬 정의)
type MgrComment = { id: string; authorName: string; ... };

// 수정
// import B2BLandingComment from Prisma client
import { B2BLandingComment } from '@prisma/client';
// 로컬 타입 정의 제거, Prisma 타입 사용
```

### Issue 9: null 체크 누락 - errorCode (라인 94)
**파일**: `src/app/api/b2b-landing/[id]/comments/route.ts`
```typescript
// 현재
const errorCode = err instanceof Error && 'code' in err ? (err as any).code : 'UNKNOWN';

// 수정
const errorCode = 
  err instanceof Error && 'code' in err && typeof (err as any).code === 'string'
    ? (err as any).code
    : 'UNKNOWN';
```

### Issue 10: startTime 중복 정의 (라인 25, 117)
**파일**: `src/app/api/b2b-landing/[id]/stats/route.ts`
```typescript
// 현재 (라인 25)
const startTime = Date.now();

// ... (라인 117에서 다시 정의)
const startTime = Date.now(); // ❌ 변수 shadowing

// 수정: 라인 117 제거, 라인 25의 startTime만 사용
// 라인 117 앞에서 사용: const duration = Date.now() - startTime;
```

### Issue 11: 타입 단언 - FormConfigType (라인 191)
**파일**: `src/app/b2b/p/[partnerId]/B2BLandingClient.tsx`
```typescript
// 현재
const fc = formConfigRef.current as FormConfigType | null;

// 수정
type FormConfigType = Record<string, unknown>;

const fc = formConfigRef.current as FormConfigType | null;
// 또는 런타임 검증 추가
if (fc && typeof fc === 'object') {
  // 안전하게 사용
}
```

### Issue 12: formConfig z.any() 제거 (new/page.tsx)
**파일**: `src/app/(dashboard)/b2b-editor/new/page.tsx`
```typescript
// 현재
z.any().optional(), // formConfig

// 수정
z.record(z.string(), z.unknown()).optional(), // formConfig
```

---

## Agent J: 코드 스멜 집중 제거 (6개 이슈)

**수정 파일**:
```
1. src/app/api/b2b-landing/route.ts (에러 처리 반복)
2. src/app/api/b2b-landing/[id]/route.ts (에러 처리 반복)
3. src/app/(dashboard)/b2b-editor/new/page.tsx (buildPreviewHtml 길이)
4. src/app/(dashboard)/b2b-editor/[id]/page.tsx (save 함수 길이)
5. src/app/b2b/p/[partnerId]/B2BLandingClient.tsx (form 필드 패턴)
6. src/app/(dashboard)/b2b-editor/page.tsx (전환율 계산 중복)
```

### Issue 13-14: 에러 처리 보일러플레이트 반복
**파일**: `src/app/api/b2b-landing/route.ts`, `[id]/route.ts`
```typescript
// 현재 (여러 곳에 반복)
if (err instanceof B2BError) {
  return NextResponse.json(
    { ok: false, error: err.code, message: err.message },
    { status: err.statusCode }
  );
}

// 수정: 유틸 함수 추출
// src/lib/b2b/response-handler.ts (신규)
export function handleB2BError(err: unknown) {
  if (err instanceof B2BError) {
    return NextResponse.json(
      { ok: false, error: err.code, message: err.message },
      { status: err.statusCode }
    );
  }
  // ... ServerError 처리
}

// 각 route에서 사용
return handleB2BError(err);
```

### Issue 15: buildPreviewHtml 길이 (131줄)
**파일**: `src/app/(dashboard)/b2b-editor/new/page.tsx`
```typescript
// 현재
const buildPreviewHtml = useCallback((): string => {
  // 131줄... 모든 섹션을 한 함수에서 렌더
}, [dependencies]);

// 수정: 섹션별 함수 분리
const buildHeaderHtml = useCallback(..., []);
const buildFormHtml = useCallback(..., []);
const buildPaymentHtml = useCallback(..., []);
const buildFooterHtml = useCallback(..., []);

const buildPreviewHtml = useCallback((): string => {
  return buildHeaderHtml() + buildFormHtml() + buildPaymentHtml() + buildFooterHtml();
}, [buildHeaderHtml, buildFormHtml, buildPaymentHtml, buildFooterHtml]);
```

### Issue 16: save() 함수 길이 (50줄)
**파일**: `src/app/(dashboard)/b2b-editor/[id]/page.tsx`
```typescript
// 현재
const save = async () => {
  // JSON 구성... 20줄
  // API 호출... 15줄
  // 에러 처리... 15줄
}

// 수정: 함수 분리
const buildPagePayload = () => { /* JSON 구성 */ };
const savePageToAPI = async (payload) => { /* API 호출 */ };
const handleSaveError = (err) => { /* 에러 처리 */ };

const save = async () => {
  const payload = buildPagePayload();
  try {
    await savePageToAPI(payload);
  } catch (err) {
    handleSaveError(err);
  }
};
```

### Issue 17: Form 필드 선택자 패턴 (라인 197-234)
**파일**: `src/app/b2b/p/[partnerId]/B2BLandingClient.tsx`
```typescript
// 현재
const name = form.querySelector('input[name="name"]') ?? form.querySelector('input[placeholder*="이름"]') ?? null;
const phone = form.querySelector('input[name="phone"]') ?? form.querySelector('input[placeholder*="전화"]') ?? null;

// 수정: 헬퍼 함수
function getFormFieldValue(form: HTMLFormElement, fieldName: string, fallbackPatterns: string[]): string {
  const input = form.querySelector(`input[name="${fieldName}"]`);
  if (input instanceof HTMLInputElement && input.value) return input.value;
  
  for (const pattern of fallbackPatterns) {
    const fallback = form.querySelector(`input[placeholder*="${pattern}"]`);
    if (fallback instanceof HTMLInputElement && fallback.value) return fallback.value;
  }
  
  return '';
}

const name = getFormFieldValue(form, 'name', ['이름', '성명', 'name']);
const phone = getFormFieldValue(form, 'phone', ['전화', 'phone', '휴대']);
```

### Issue 18: 전환율 계산 중복 (라인 185-195)
**파일**: `src/app/(dashboard)/b2b-editor/page.tsx`
```typescript
// 현재
(page._count.registrations / page.viewCount) >= 0.05 ? "..." :
(page._count.registrations / page.viewCount) >= 0.02 ? "..." : "..."

// 수정
const conversionRate = page._count.registrations / page.viewCount;
const status = 
  conversionRate >= 0.05 ? "..."  :
  conversionRate >= 0.02 ? "..." :
  "...";
```

---

## Agent K: 테스트 + 보안 강화 (6개 이슈)

**수정 파일**:
```
1. src/app/(dashboard)/b2b-editor/[id]/page.tsx (테스트 케이스)
2. src/app/api/b2b-landing/[id]/comments/generate/route.ts (테스트)
3. src/app/api/b2b-landing/[id]/comments/route.ts (테스트)
4. src/app/b2b/p/[partnerId]/B2BLandingClient.tsx (테스트)
5. src/app/api/b2b-landing/[id]/comments/generate/route.ts (보안)
6. src/app/b2b/p/[partnerId]/B2BLandingClient.tsx (보안)
```

### Issue 25: 테스트 - 빈 이미지 배열 엣지 케이스
**파일**: `src/app/(dashboard)/b2b-editor/[id]/page.tsx`
```typescript
// 추가: 테스트 시나리오
// src/tests/b2b-editor-upload.test.ts (신규)
describe('Image Upload Edge Cases', () => {
  it('should handle empty file selection', () => {
    const files = new FileList(); // 빈 배열
    const validFiles = Array.from(files).filter(f => f.type.startsWith('image/'));
    expect(validFiles).toHaveLength(0);
  });
  
  it('should reject non-image files', () => {
    // ...
  });
  
  it('should handle partial failures gracefully', () => {
    // ...
  });
});
```

### Issue 26: 테스트 - JSON 파싱 실패
**파일**: `src/app/api/b2b-landing/[id]/comments/generate/route.ts`
```typescript
// 추가: 테스트
describe('Claude Comment Generation', () => {
  it('should handle JSON parse errors', async () => {
    // Claude 응답이 유효한 JSON 아닐 때
    const res = await POST(req, { params: Promise.resolve({ id: '123' }) });
    expect(res.status).toBe(500);
  });
  
  it('should retry on transient errors', async () => {
    // ...
  });
});
```

### Issue 27: 테스트 - 댓글 경계값
**파일**: `src/app/api/b2b-landing/[id]/comments/route.ts`
```typescript
// 추가: 경계값 테스트
describe('Comments Pagination', () => {
  it('should clamp skip to max 10000', async () => {
    const req = new Request('?skip=999999999');
    // ...
    expect(skip).toBeLessThanOrEqual(10000);
  });
  
  it('should handle limit=0', () => {
    const limit = Math.min(50, Math.max(1, 0 || 10));
    expect(limit).toBe(10); // 폴백값
  });
  
  it('should handle negative values', () => {
    // ...
  });
});
```

### Issue 28: 테스트 - 동시성 (이미지 + 저장)
**파일**: `src/app/(dashboard)/b2b-editor/[id]/page.tsx`
```typescript
// 추가: 동시성 시나리오
describe('Image Upload + Save Race Condition', () => {
  it('should not lose images on concurrent save', async () => {
    const upload = uploadFiles([file1, file2]);
    const save = savePageData(); // 동시 실행
    
    const [images, pageData] = await Promise.all([upload, save]);
    expect(pageData.images).toEqual(images); // 일관성 보장
  });
});
```

### Issue 30: 보안 - Rate Limit 우회 (라인 49)
**파일**: `src/app/api/b2b-landing/[id]/comments/generate/route.ts`
```typescript
// 현재
const rateLimitKey = `b2b:comments:generate:${orgId}:${id}`;

// 수정: 요청 해시 추가 (IP 또는 user-agent)
import crypto from 'crypto';

const clientFingerprint = crypto
  .createHash('sha256')
  .update(`${req.headers.get('x-forwarded-for') || 'unknown'}:${req.headers.get('user-agent') || ''}`)
  .digest('hex')
  .slice(0, 8);

const rateLimitKey = `b2b:comments:generate:${orgId}:${id}:${clientFingerprint}`;
```

### Issue 31: 보안 - 입력 검증 엣지 (라인 244-245)
**파일**: `src/app/b2b/p/[partnerId]/B2BLandingClient.tsx`
```typescript
// 현재
const rawPhone = phoneVal.replace(/[^0-9]/g, "");
if (rawPhone && !/^01[016789]\d{7,8}$/.test(rawPhone)) { ... }

// 문제: 공백만 입력하면 rawPhone === "", falsy로 판정되어 검증 스킵

// 수정
const rawPhone = phoneVal.trim().replace(/[^0-9]/g, "");
if (!rawPhone) {
  return handleError('전화번호를 입력하세요');
}
if (!/^01[016789]\d{7,8}$/.test(rawPhone)) {
  return handleError('유효한 전화번호가 아닙니다');
}
```

---

## 검증 체크리스트

- ✅ 각 에이전트 독립적 작업 (겹치지 않는 파일)
- ✅ 타입 안전성: any → 구체적 타입 지정
- ✅ 코드 스멜: 중복 제거, 함수 분리
- ✅ 테스트: 엣지 케이스, 동시성, 경계값
- ✅ 보안: 검증 강화, Rate limit 우회 방지

---

## 예상 결과

- **타입 에러**: 0개 (any 제거)
- **코드 복잡도**: 10% ↓ (함수 분리)
- **테스트 커버리지**: 60%+ (엣지 케이스)
- **보안 이슈**: 0개 (입력 검증, Rate limit)
- **유지보수성**: 대폭 개선 (코드 스멜 제거)

---

## 다음: Wave 4 (P3 백로그, ~20개)

