import { ReactNode } from 'react';

export const metadata = {
  title: 'Loop 5 성과 대시보드 | mabiz CRM',
  description: '실시간 SMS 캠페인 성과 추적 및 최적화 대시보드',
};

export default function Loop5Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
