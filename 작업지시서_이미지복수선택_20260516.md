# 🎯 작업지시서: 랜딩페이지 이미지 복수선택 기능

**작업명:** 랜딩페이지 이미지 라이브러리 + 파일 업로드 복수선택 기능
**날짜:** 2026-05-16
**우선순위:** P1 (성능/속도 개선)
**예상 시간:** 4시간

---

## 📋 10렌즈 코드 리뷰 결과

### 1️⃣ **성능 렌즈** ⚠️ P0
**문제점:**
```typescript
// 현재: 순차 처리 (10개 파일 = 10번 API 호출)
for (let i = 0; i < files.length; i++) {
  await fetch("/api/landing-pages/images", ...);  // 하나 끝날 때까지 대기
}
```
**개선:** 3~5개씩 배치 처리로 **80% 시간 단축** 가능

---

### 2️⃣ **보안 렌즈** ✅ OK
**현재 상태:**
- ✅ 파일 타입 검증 (이미지만)
- ✅ 크기 제한 (20MB)
- ✅ landingPageId 소유권 확인 (백엔드)

---

### 3️⃣ **사용자 경험 렌즈** ⚠️ P1
**문제점:**
- ❌ 진행률 표시 없음 (업로드 중 답답함)
- ❌ 부분 실패 시 **마지막 에러만 표시** (3/10 성공해도 모름)
- ❌ 파일 개수 표시 없음 ("5개 선택" 같은 안내 부재)

**개선:**
```
업로드 중... (3/10) 30%
✅ image1.jpg 완료
⏳ image2.jpg 처리 중...
❌ image3.jpg 실패 (파일 크기 초과)
```

---

### 4️⃣ **확장성 렌즈** ⚠️ P1
**문제점:**
- ❌ Drive 라이브러리 이미지 추가 기능 전혀 없음
- ❌ 라이브러리의 체크박스 선택 로직 미구현

**개선:** 모달 창으로 라이브러리 이미지 복수 선택 + 한 번에 추가

---

### 5️⃣ **유지보수성 렌즈** ⚠️ P2
**문제점:**
```typescript
// uploadFiles() 함수가 너무 많은 책임
// - 파일 검증
// - 정렬 계산
// - API 호출
// - 상태 관리
// - 에러 처리
```

**개선:** 함수 분리
```typescript
validateFiles()        // 파일 검증
batchUpload()         // 배치 처리 로직
calculateSortOrder()  // 정렬 계산
```

---

### 6️⃣ **속도/효율 렌즈** ⚠️ P0
**문제점:**
```typescript
// 현재 10개 파일 = 약 10-20초 소요
// 배치 처리 시 = 약 3-5초 소요 (80% 개선)
```

**개선:** 병렬 처리 (Promise.all 사용)

---

### 7️⃣ **DB 최적화 렌즈** ⚠️ P2
**문제점:**
```typescript
formData.append("sortOrder", String(images.length + i));  // 계산 오류 가능성
```
- 만약 업로드 중 다른 이미지가 추가되면 순서 꼬임
- 백엔드에서 안전하게 계산하는 게 나음

**개선:** sortOrder는 백엔드에서 자동 계산

---

### 8️⃣ **에러 처리 렌즈** ⚠️ P1
**문제점:**
```typescript
else setError(data.message ?? `${file.name} 업로드 실패`);  // 마지막만 표시
```
3/10 성공해도 "1개 실패" 메시지만 보임

**개선:** 성공/실패 배열 분리
```typescript
{
  success: [image1, image2, image3],
  failed: [{ fileName: 'image4.jpg', reason: '20MB 초과' }]
}
```

---

### 9️⃣ **접근성 렌즈** ⚠️ P2
**문제점:**
```typescript
alt="랜딩페이지 이미지"  // 모두 동일한 alt text
```

**개선:** 이미지별 alt text 동적 설정
```typescript
alt={img.altText || `${img.fileName} (${idx + 1}/${images.length})`}
```

---

### 🔟 **비용 최적화 렌즈** ⚠️ P3
**문제점:**
- Drive 권한 설정이 매 이미지마다 호출됨
- 배치 처리로 최대 5배 감소 가능

**개선:** 배치 처리 시 권한 설정도 최소화

---

## 🛠️ 구현 태스크

### Phase 1: 파일 업로드 배치 처리 (1.5시간)

**파일:** `src/app/(dashboard)/landing-pages/[id]/page.tsx`

#### Task 1-1: 배치 업로드 함수 작성
```typescript
// 새로운 함수 추가
async function batchUploadFiles(files: File[], maxConcurrent: number = 3) {
  const results = { success: [], failed: [] };
  const queue = Array.from(files);
  
  while (queue.length > 0) {
    const batch = queue.splice(0, maxConcurrent);
    const batchResults = await Promise.allSettled(
      batch.map(file => uploadSingleFile(file))
    );
    // 결과 수집
  }
  
  return results;
}
```

**체크리스트:**
- [ ] 3~5개씩 배치 처리
- [ ] 성공/실패 분리
- [ ] 진행률 계산 (예: 3/10)
- [ ] 각 파일별 상태 표시

---

#### Task 1-2: UI 개선 (파일 업로드)
**파일:** `src/app/(dashboard)/landing-pages/[id]/page.tsx` (730~763줄)

변경사항:
```typescript
// Before
<p className="text-sm font-medium text-gray-600">{uploading ? "업로드 중..." : "이미지를 드래그하거나..."}</p>

// After
<p className="text-sm font-medium text-gray-600">
  {uploading 
    ? `업로드 중... (${uploadProgress.processed}/${uploadProgress.total}) ${uploadProgress.percent}%`
    : "이미지를 드래그하거나 클릭하여 업로드"}
</p>
<p className="text-xs text-gray-500 mt-2">
  💡 팁: 최대 5개씩 선택하면 빠릅니다. (3~5개 동시 처리)
</p>

// 부분 성공 메시지
{uploadResults && (
  <div className="mt-4 space-y-2">
    <p className="text-sm text-green-600">✅ {uploadResults.success.length}개 성공</p>
    {uploadResults.failed.length > 0 && (
      <div className="text-sm text-red-600">
        <p>❌ {uploadResults.failed.length}개 실패:</p>
        <ul className="ml-4">
          {uploadResults.failed.map(f => (
            <li key={f.fileName}>- {f.fileName}: {f.reason}</li>
          ))}
        </ul>
      </div>
    )}
  </div>
)}
```

**체크리스트:**
- [ ] 선택된 파일 개수 표시
- [ ] 배치 처리 팁 문구 추가
- [ ] 진행률 표시 (예: 3/10, 30%)
- [ ] 성공/실패 분리 메시지

---

### Phase 2: Drive 라이브러리 모달 (2시간)

#### Task 2-1: 모달 컴포넌트 생성
**파일 생성:** `src/app/(dashboard)/landing-pages/[id]/components/ImageLibraryModal.tsx`

```typescript
interface ImageLibraryModalProps {
  isOpen: boolean;
  onClose: () => void;
  onSelect: (assetIds: string[]) => void;  // 선택 순서대로
  landingPageId: string;
}

export function ImageLibraryModal(props: ImageLibraryModalProps) {
  const [assets, setAssets] = useState([]);
  const [selectedIds, setSelectedIds] = useState<string[]>([]);  // 순서 유지
  const [selectedCategory, setSelectedCategory] = useState('');
  // ... 라이브러리 페이지와 동일한 로직
  
  return (
    <Dialog open={isOpen} onOpenChange={onClose}>
      {/* 카테고리 필터 + 검색 + 체크박스 그리드 */}
      {/* 하단: "추가" 버튼 (선택된 이미지 개수 표시) */}
    </Dialog>
  );
}
```

**체크리스트:**
- [ ] 카테고리 필터 구현
- [ ] 검색 기능
- [ ] 체크박스 (다중선택, 순서 유지)
- [ ] "3/100 선택" 표시
- [ ] "추가" 버튼 (선택 없으면 비활성화)

---

#### Task 2-2: 라이브러리 이미지 추가 API
**파일:** `src/app/api/landing-pages/[id]/images/add-from-library/route.ts`

```typescript
// POST /api/landing-pages/[id]/images/add-from-library
// Body: { assetIds: string[] }  // 선택된 이미지 ID들 (순서 유지)
// Response: { ok: boolean, images: UploadedImage[] }

export async function POST(req: Request, { params }: { params: { id: string } }) {
  const { assetIds } = await req.json();
  
  // 1. 랜딩페이지 소유권 확인
  // 2. 각 assetId가 유효한지 확인
  // 3. CrmLandingPageImage 레코드 생성 (sortOrder 자동 계산)
  // 4. 반환
}
```

**체크리스트:**
- [ ] 소유권 확인
- [ ] 자산 유효성 검증
- [ ] sortOrder 자동 계산 (기존 이미지 개수 + 순서)
- [ ] 중복 추가 방지 (@@unique([landingPageId, imageAssetId]))
- [ ] 배치 처리 (3~5개씩)

---

#### Task 2-3: 페이지에 모달 통합
**파일:** `src/app/(dashboard)/landing-pages/[id]/page.tsx`

추가 버튼:
```typescript
// 이미지 업로드 UI 상단에 추가
<button
  onClick={() => setLibraryModalOpen(true)}
  className="mb-4 px-4 py-2 bg-blue-600 text-white rounded-lg text-sm font-medium hover:bg-blue-700"
>
  📚 Drive 라이브러리에서 추가
</button>

{/* 모달 */}
<ImageLibraryModal
  isOpen={libraryModalOpen}
  onClose={() => setLibraryModalOpen(false)}
  onSelect={async (assetIds) => {
    // API 호출 → 이미지 추가
    const res = await fetch(`/api/landing-pages/${id}/images/add-from-library`, {
      method: 'POST',
      body: JSON.stringify({ assetIds })
    });
    // 결과 처리
  }}
  landingPageId={id}
/>
```

**체크리스트:**
- [ ] 버튼 UI 추가
- [ ] 모달 열기/닫기
- [ ] API 호출 (배치 처리)
- [ ] 성공/실패 메시지 표시

---

## 📊 예상 영향

| 항목 | Before | After | 개선 |
|------|--------|-------|------|
| 10개 파일 업로드 시간 | ~15초 | ~3초 | **80% ↓** |
| API 호출 수 | 10 | 2~3 | **70% ↓** |
| UX (진행률 표시) | ❌ 없음 | ✅ 있음 | **체감 속도 향상** |
| 기능 (라이브러리 연동) | ❌ 없음 | ✅ 있음 | **사용 편의성 ↑** |
| 에러 메시지 | 마지막만 | 전부 | **디버깅 쉬움** |

---

## 🎓 학습 포인트

1. **배치 처리**: Promise.allSettled()로 병렬 처리
2. **선택 순서 유지**: 배열로 순서 보존
3. **부분 성공 처리**: success/failed 분리
4. **모달 컴포넌트**: 재사용 가능하게 설계
5. **sortOrder 안전성**: 백엔드에서 자동 계산

---

## ✅ 완료 기준

- [ ] 파일 업로드 배치 처리 (3~5개씩)
- [ ] 진행률 표시 (예: 3/10, 30%)
- [ ] 부분 성공 메시지 (성공/실패 분리)
- [ ] Drive 라이브러리 모달 (카테고리/검색/선택)
- [ ] 라이브러리 이미지 추가 API
- [ ] 페이지 통합 + 테스트
- [ ] 모든 기능이 정상 작동하는지 수동 테스트

---

## 🚀 배포 체크리스트

- [ ] 로컬 테스트 완료 (파일 업로드 + 라이브러리 추가)
- [ ] 다양한 파일 크기 테스트 (1MB, 10MB, 20MB)
- [ ] 에러 케이스 테스트 (파일 타입, 크기 초과, 네트워크 오류)
- [ ] 성능 프로파일링 (Time to Complete)
- [ ] 커밋 및 PR 작성 (메모리 파일에도 저장)
