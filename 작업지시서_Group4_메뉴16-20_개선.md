# Group4 (메뉴 #16-20) 종합 개선 작업지시서

**작성 일자**: 2026-05-17  
**범위**: 급여명세, 커미션원장, 연말정산, 상품관리, 회원관리 (5개 메뉴)  
**총 이슈**: 48개 (P0: 4개, P1: 16개, P2: 19개, P3: 9개)  
**예상 시간**: 20-24시간  

---

## Step 2: 10렌즈 토론 (근본 원인 분석)

### 메뉴 #16 급여명세 (payslips)

#### 발견된 이슈 (8개)

| ID | 심각도 | 렌즈 | 문제 | 원인 |
|----|----|----|----|-----|
| P16-01 | P0 | Business | bonus null 안전성 부족 | AffiliatePayslip 스키마에 bonus 컬럼 미존재 → null 반환 후 UI에서 "-" 표시, 비즈니스 정의 불명확 |
| P16-02 | P1 | API | STATUS_LABELS 매핑 오류 | STATUS_LABELS["PENDING"/"APPROVED"/"SENT"] 정의했으나 UI에서 ?. 연산자 없이 접근하면 undefined 가능 |
| P16-03 | P1 | Data | totalPages 계산 누락 | API 응답에 totalPages 없음, FE에서 직접 계산 (Math.ceil) 하지만 API 응답 일관성 필요 |
| P16-04 | P1 | Performance | 쿼리 최적화 미흡 | JOIN 3개, 인덱스 활용 미확인, 기간 필터링 LIKE 연산자 사용 가능 |
| P16-05 | P2 | UX | 에러 응답 불명확 | fetch catch에서 console.error만 출력, 사용자 피드백 없음 |
| P16-06 | P2 | Testing | 권한 검증 불완전 | agentProfileId null 체크 있으나, 쿼리에서 profile 관계 검증 미흡 |
| P16-07 | P2 | Dependencies | 일관성 없는 필드명 | yearMonth vs period 혼용 (API는 period, UI는 yearMonth), 매핑 미확인 |
| P16-08 | P3 | Code | 상수 정의 분산 | STATUS_LABELS를 페이지에 정의, 유틸화 필요 |

#### 10렌즈 분석

**1. Business**: 수당 구조가 GMcruise AffiliatePayslip(totalCommission, totalWithholding, netPayment)인데, UI는 baseCommission, bonus, deduction, netAmount 필드 기대 → **역할 매핑 문서 필요**

**2. Data**: bonus=null은 AffiliatePayslip에 별도 bonus 컬럼이 없으므로 설계 상 맞지만, UI 로직에서 ?? 연산자 누락 가능 → **null 안전성 강화**

**3. API**: yearMonth (기간 필터) 형식 검증 (YYYY-MM) 있으나, API 응답 totalPages 계산을 FE에 위임 → **응답 표준화 필요**

**4. Performance**: 쿼리는 기간별 LIKE 연산자 대신 범위 쿼리 추천

**5. Security**: profileId 스코프 조건이 SQL 파라미터화됐으므로 SQL 인젝션 안전, 하지만 논리적 권한 검증 강화 필요

**6. Testing**: role별 권한 검증(FREE_SALES 403, AGENT 자신만, OWNER 소속만) → 테스트 시나리오 필요

---

### 메뉴 #17 커미션원장 (commission-ledger)

#### 발견된 이슈 (12개)

| ID | 심각도 | 렌즈 | 문제 | 원인 |
|----|----|----|----|-----|
| P17-01 | P0 | Data | WITHHOLDING 부호 오류 | formatAmount()에서 WITHHOLDING만 negative 처리 하지만, DB 값이 이미 음수인지 양수인지 불명확 → 중복 음수화 위험 |
| P17-02 | P1 | Data | yearMonth 필터링 미포함 | yearMonth 파라미터 입력받으나 쿼리 조건에서 범위 계산만 함, 응답 yearMonth는 createdAt 파생 → 불일치 가능 |
| P17-03 | P1 | API | typeFilter 유효성 검증 미흡 | ALLOWED_TYPES 정의했으나, 쿼리 조건 WHERE절에서 명시적으로 LIKE 비교 없음 → SQL 인젝션 위험은 없으나 논리 검증 필요 |
| P17-04 | P1 | Performance | monthRange() 활용 미흡 | 월별 범위 계산은 정확하나, 쿼리 OFFSET 페이지네이션이 느릴 수 있음 (KEYSET pagination 추천) |
| P17-05 | P1 | UX | 에러 메시지 불명확 | catch에서 console.error만 출력, 사용자 피드백 없음 |
| P17-06 | P2 | Code | 중복 정의 | TYPE_META, YM_OPTIONS 페이지와 API에서 각각 정의 → 유틸 파일로 통합 필요 |
| P17-07 | P2 | Data | balance 누적 계산 검증 | SUM() OVER 윈도우 함수로 누적 계산하나, 역할별 scope가 다르면 balance 불일치 가능 |
| P17-08 | P2 | Testing | yearMonth 필터 테스트 미흡 | 월 경계(12월→1월) 케이스 확인 필요 |
| P17-09 | P2 | Performance | Summary 쿼리 최적화 | 3개 쿼리(ledger, count, summary) 동시 실행하나, summary는 단순 SUM이므로 ledger 쿼리 후 메모리 집계 가능 |
| P17-10 | P3 | Code | 상수 정의 분산 | TYPE_META를 페이지에만 정의, API응답과 UI가 불일치 가능 |
| P17-11 | P3 | Data | withholding amount 필드 미사용 | API 응답에 withholdingAmount 필드 있으나 UI에서 사용하지 않음 → 제거 또는 표시 필요 |
| P17-12 | P3 | UX | 소수점 처리 | formatAmount()에서 toLocaleString 사용하나 소수점 여부 미확인 |

#### 10렌즈 분석

**1. Business**: WITHHOLDING 항목의 부호(양수/음수)가 명확하지 않음. API에서 SUM(amount) 계산할 때 음수여야 올바른 net 계산 가능 → **스키마 명확화 필요**

**2. Data**: yearMonth 범위 필터링이 createdAt 범위로 변환되므로, 요청한 yearMonth와 응답 yearMonth 일치 검증 필요

**3. API**: typeFilter 검증은 있으나 쿼리 조건 WHERE절에서 직접 파라미터 사용 안 함, 오직 roleCondition/profileCondition만 사용 → 논리 오류

**4. Performance**: offset 기반 페이지네이션 → KEYSET(createdAt DESC) 페이지네이션 추천

**5. Security**: 권한 검증은 roleCondition/profileCondition으로 보호됨

**6. Testing**: yearMonth 필터, typeFilter 필터, summary 계산 정확성 테스트 필요

---

### 메뉴 #18 연말정산 (year-end-report)

#### 발견된 이슈 (9개)

| ID | 심각도 | 렌즈 | 문제 | 원인 |
|----|----|----|----|-----|
| P18-01 | P0 | Code | TODO 주석 미완성 | 라인 93: "TODO: FREE_SALES 수당 포함 필요" → 구현되지 않음, unsetCommissionCount 집계만 함 |
| P18-02 | P0 | Data | unsetCommissionCount 누락 | API에서 계산하나 UI에서 표시하지 않음 → 데이터 낭비 |
| P18-03 | P0 | Testing | refundRate 계산 검증 | totalSaleAmount = 0일 때 refundRate = 0 반환하나, division by zero 확인 필요 |
| P18-04 | P1 | API | refundRate 소수점 처리 | Math.round(...\*1000)/10 로 소수점 1자리 처리, but UI에서 toFixed(1) 또 처리 → 일관성 부족 |
| P18-05 | P1 | Performance | 쿼리 최적화 미흡 | LEFT JOIN AffiliateSale은 괜찮으나, yearMonth LIKE 연산자 → 범위 쿼리로 변경 권장 |
| P18-06 | P1 | Data | grandTotal 계산 누락 | agents 배열의 unsetCommissionCount를 합계하지 않음 → UI에서 사용 불가 |
| P18-07 | P2 | UX | 환불율 경고 표시 | 환불율 >= 30% 일 때 "!" 배지 표시하나, 임계값 기준 명확하지 않음 |
| P18-08 | P2 | Code | RANK_STYLES/RANK_LABEL_STYLES 하드코딩 | 순위별 스타일 정의했으나 4위 이상은 기본값, 확장성 부족 |
| P18-09 | P3 | Data | affiliateCode 기반 FREE_SALES 미포함 | AffiliateSale.affiliateCode로 별도 집계 필요하나 미구현 |

#### 10렌즈 분석

**1. Business**: FREE_SALES 판매원 수당이 포함되지 않음 (TODO 주석), 연말정산 보고서 불완전 → **FREE_SALES 집계 로직 필요**

**2. Data**: unsetCommissionCount(수당 미정 건수)를 계산하지만 UI에 표시하지 않음 → 이것이 중요 KPI라면 표시 필요, 아니면 제거

**3. API**: refundRate 소수점 처리가 API와 UI에서 각각 처리 → 통일 필요

**4. Code**: RANK_STYLES/RANK_LABEL_STYLES 상수 정의했으나, 4위 이상은 기본값 처리 → 유연성 부족

**5. Testing**: division by zero 방지 (totalSaleAmount=0일 때 refundRate=0), FREE_SALES 포함 시 집계 정확성 테스트

**6. Dependencies**: 현재 GMcruise AffiliateProfile + AffiliateSale만 조회, FreeSales 테이블이 있는지 확인 필요

---

### 메뉴 #19 상품관리 (products)

#### 발견된 이슈 (10개)

| ID | 심각도 | 렌즈 | 문제 | 원인 |
|----|----|----|----|-----|
| P19-01 | P0 | Code | RefreshCw 아이콘 임포트 누락 | 라인 11에 RefreshCw 없음, 객실등록 모달에서 사용 가능성 높음 |
| P19-02 | P0 | API | 환불정책 계산 로직 오류 | refundPolicy.slots[i].daysBeforeDep와 daysLeft 비교 시, daysBeforeDep > daysLeft면 적용 가능하나 로직 미정의 |
| P19-03 | P0 | Performance | 무한 로딩 가능성 | 객실 모달에서 cabinSummary 업데이트 후 revalidatePath 없음 → 페이지 새로고침 필요 |
| P19-04 | P1 | Data | KST 타임존 하드코딩 | formatDate()에서 UTC 기준이나, 한국 출발 기준 변경 시 D-day 계산 오류 |
| P19-05 | P1 | UX | daysLeft null 처리 부족 | DdayBadge에서 null 체크만 하고 "-" 표시, but 실제 계산 로직 미확인 |
| P19-06 | P1 | Testing | 객실 인벤토리 테스트 | 마감(remaining=0) 처리, 3개 이하 경고 표시, 객실 타입별 누적 정확성 |
| P19-07 | P2 | Code | CABIN_TYPE_CONFIG 재사용성 낮음 | 하드코딩된 객실 타입, 별도 관리 페이지 추가 시 유지보수 어려움 |
| P19-08 | P2 | UX | 마일스톤 배지 임계값 불명확 | D-30~365 범위에서 3/6/9/12개월 구분하나, 비즈니스 정책 미명확 |
| P19-09 | P2 | Data | cabinSummary 데이터 구조 | Record<string, CabinEntry> 유연하나, 실제 필드 타입 검증 미흡 |
| P19-10 | P3 | Performance | CabinSummaryCell 렌더링 최적화 | known/extra 배열 생성 후 map, but 모든 제품에 객실이 없으면 "객실 등록" 버튼만 표시 → 최적화 여지 있음 |

#### 10렌즈 분석

**1. Business**: 객실 인벤토리 관리가 핵심, daysLeft 기반 D-day 배지와 환불정책 계산이 복잡 → **명확한 비즈니스 규칙 문서 필요**

**2. Data**: refundPolicy JSON 구조가 유연하나 검증 부족, CabinSummary 필드명 일관성 확인 필요

**3. API**: 환불정책 계산 로직(daysBeforeDep vs daysLeft 비교) 미정의 → **계산 알고리즘 명확화**

**4. Code**: RefreshCw 임포트 누락, 상수(CABIN_TYPE_CONFIG) 분산 정의

**5. Performance**: 객실 업데이트 후 캐시 무효화(revalidatePath), daysLeft 계산 최적화 (서버 → 클라이언트 변경 고려)

**6. Testing**: D-day 경계값 (0, 1, 7, 30, 365), 환불율 적용 시나리오, 객실 마감 처리

---

### 메뉴 #20 회원관리 (members)

#### 발견된 이슈 (9개)

| ID | 심각도 | 렌즈 | 문제 | 원인 |
|----|----|----|----|-----|
| P20-01 | P0 | UX | 담당자 지정 optimistic update 없음 | assignStaff() 호출 후 상태 업데이트 지연, 사용자가 완료 확인 못 함 |
| P20-02 | P0 | Data | 권한 검증 미흡 | setSelectedMember() 호출 시 memberStatus/tags 초기화만 하고, 실제 권한(OWNER/AGENT) 검증 미흡 |
| P20-03 | P1 | Data | groups 조회 지연 | groupsLoading 상태가 있으나 groups 초기화 시점이 불명확 → 모달 열 때마다 조회 필요 |
| P20-04 | P1 | API | 그룹 생성 API 응답 검증 | creatingGroup 상태 하지만, 응답 필드 검증 미흡 (groupId, groupName) |
| P20-05 | P1 | UX | 태그 입력 인터페이스 분명하지 않음 | SUGGEST_TAGS 정의했으나 자동완성 UI 미확인 |
| P20-06 | P2 | Code | 상태/태그/그룹 동기화 복잡 | memberStatus, memberTags, selectedGroups 3가지 상태 병렬 관리 → 변경 이력 기록 누락 가능 |
| P20-07 | P2 | Testing | 담당자 지정 → 변경이력 연동 테스트 | assignStaff() 후 changeHistory 갱신 여부 확인 필요 |
| P20-08 | P2 | UX | 상태 업데이트 메시지 불명확 | statusUpdateMsg 상태 있으나 실제 사용 시점/메시지 내용 미정의 |
| P20-09 | P3 | Data | groups 필드 타입 | groups?: 옵션 필드, 조회 시점 명확하지 않음 |

#### 10렌즈 분석

**1. Business**: 회원 담당자 지정이 핵심, 상태/태그/그룹 세그먼테이션까지 복합 → **담당자 지정 프로세스 문서화 필요**

**2. Data**: memberStatus, memberTags, selectedGroups이 병렬 관리되므로, 이들 간 일관성 보장 필요 (예: 태그 추가 시 상태 변경 안 함)

**3. API**: 담당자 지정 API (assignStaff), 그룹 생성 API 응답 검증 부족

**4. UX**: optimistic update 부족 (사용자가 완료를 알 수 없음), 태그 자동완성 UI 미확인

**5. Security**: 권한 검증 (OWNER/AGENT 확인), 담당자 지정 권한 제한 필요

**6. Testing**: 담당자 지정 → changeHistory 연동, 상태/태그/그룹 변경 이력 기록, 권한 별 접근 제어

---

## Step 3: 작업지시서 (P0 + P1 우선)

### P0 이슈 (4개) — 1-2일 내 필수 완료

#### 1. 급여명세: bonus null 안전성 강화 (P16-01)

**파일**: `src/app/(dashboard)/payslips/page.tsx:155`  
**문제**: `p.bonus != null`로 null 체크하나, 실제 bonus 필드가 항상 null (AffiliatePayslip 스키마 미존재)  
**수정**:
```tsx
// 기존
{p.bonus != null ? `+${p.bonus.toLocaleString()}원` : "-"}

// 수정
{p.bonus !== null && p.bonus !== undefined ? `+${p.bonus.toLocaleString()}원` : "-"}
```

**또는** API 응답에서 bonus 제거 (항상 null) 후 UI에서 필드 삭제

**예상시간**: 10분

---

#### 2. 커미션원장: WITHHOLDING 부호 오류 (P17-01)

**파일**: `src/app/(dashboard)/commission-ledger/page.tsx:39-46` + `src/app/api/commission-ledger/route.ts:131-137`  
**문제**: formatAmount()에서 WITHHOLDING을 negative 처리하나, DB 저장된 amount가 이미 음수인지 양수인지 불명확 → 중복 음수화 위험

**조사 필요**:
1. DB `CommissionLedger` 테이블의 WITHHOLDING amount 부호 확인
2. 집계 쿼리에서 totalWithholding 계산 로직 확인 (현재 SUM(amount))

**수정**:
```tsx
// API: CommissionLedger 스키마에서 WITHHOLDING 부호 명시
// (음수 저장 OR 양수 저장) → 문서화

// UI: formatAmount() 개선
function formatAmount(type: string, amount: number) {
  const absAmount = Math.abs(amount);
  const isNegative = type === "WITHHOLDING" || amount < 0;
  return (
    <span className={isNegative ? "text-red-500" : "text-green-600"}>
      {isNegative ? "-" : "+"}
      {absAmount.toLocaleString("ko-KR")}원
    </span>
  );
}
```

**예상시간**: 20분

---

#### 3. 연말정산: TODO 주석 미완성 (P18-01)

**파일**: `src/app/api/year-end-report/route.ts:93`  
**문제**: "TODO: FREE_SALES 수당 포함 필요" → 미구현, UI에서 수당이 불완전

**수정**:
1. FREE_SALES 판매원 데이터 출처 확인 (AffiliateSale.affiliateCode? GoldMember?)
2. 별도 쿼리 추가: FREE_SALES 판매원 수당 집계
3. 최종 결과에 병합

```sql
-- 추가 쿼리: FREE_SALES 수당 (affiliateCode 기반)
SELECT
  COALESCE(SUM(CASE WHEN status IN ('APPROVED','CONFIRMED') THEN "saleAmount" ELSE 0 END), 0)::bigint AS "totalSaleAmount",
  COALESCE(SUM(CASE WHEN status IN ('APPROVED','CONFIRMED') THEN "salesCommission" ELSE 0 END), 0)::bigint AS "totalCommission",
  COUNT(CASE WHEN status IN ('APPROVED','CONFIRMED') THEN 1 END)::bigint AS "confirmedCount"
FROM "AffiliateSale"
WHERE "affiliateCode" IN (...)
  AND "yearMonth" LIKE ${yearLike}
GROUP BY "affiliateCode"
```

**예상시간**: 1시간

---

#### 4. 상품관리: RefreshCw 아이콘 임포트 누락 (P19-01)

**파일**: `src/app/(dashboard)/products/page.tsx:11`  
**문제**: 객실 등록 모달에서 RefreshCw 사용할 가능성 높으나 임포트 없음

**수정**:
```tsx
// 기존
import {
  ShoppingBag,
  ChevronLeft,
  ChevronRight,
  Search,
  Download,
  Calculator,
  X,
  PlusCircle,
  MapPin,
  FileSpreadsheet,
  Loader2,
} from "lucide-react";

// 수정
import {
  ShoppingBag,
  ChevronLeft,
  ChevronRight,
  Search,
  Download,
  Calculator,
  X,
  PlusCircle,
  MapPin,
  FileSpreadsheet,
  Loader2,
  RefreshCw,  // 추가
} from "lucide-react";
```

**예상시간**: 5분

---

### P1 이슈 (16개) — 이 주 내 완료 권장

#### 1. 급여명세: STATUS_LABELS 매핑 안전성 (P16-02)

**파일**: `src/app/(dashboard)/payslips/page.tsx:98, 143`  
**문제**: `STATUS_LABELS[s]?.label` 연산자로 안전하나, 기본값 처리 불명확

**수정**:
```tsx
// 기존
status === "" ? "전체" : (STATUS_LABELS[s]?.label ?? s)

// 수정
status === "" ? "전체" : (STATUS_LABELS[s as keyof typeof STATUS_LABELS]?.label ?? s)
// 또는
status === "" ? "전체" : (STATUS_LABELS[status]?.label ?? status)
```

**예상시간**: 10분

---

#### 2. 커미션원장: yearMonth 필터링 로직 확인 (P17-02)

**파일**: `src/app/api/commission-ledger/route.ts:97-102`  
**문제**: yearMonth 파라미터로 범위(start, end) 계산하나, 응답 yearMonth는 createdAt 파생 → 요청과 응답 불일치 가능

**수정**:
```tsx
// 응답에 요청한 yearMonth 포함
return NextResponse.json({
  ok: true,
  ledger,
  requestedYearMonth: yearMonth,  // 추가
  summary: { ... },
  total,
  page,
  totalPages: Math.ceil(total / limit),
});
```

**또는** 응답의 ledger.yearMonth를 응청 yearMonth로 재계산하여 일관성 보장

**예상시간**: 15분

---

#### 3. 커미션원장: typeFilter 쿼리 조건 반영 (P17-03)

**파일**: `src/app/api/commission-ledger/route.ts:104-106`  
**문제**: typeFilter 검증은 있으나 쿼리 WHERE절에 포함 안 됨 → UI 필터가 무시됨

**수정**:
```tsx
// 기존: typeCondition 정의했으나 쿼리에서 사용 안 함
const typeCondition: Prisma.Sql = typeFilter
  ? Prisma.sql`AND cl."entryType" = ${typeFilter}`
  : Prisma.empty;

// 쿼리에 반영 확인
prisma.$queryRaw<RawLedger[]>(Prisma.sql`
  SELECT ...
  FROM "CommissionLedger" cl
  WHERE 1=1
    ${roleCondition} ${profileCondition} ${yearMonthCondition} ${typeCondition}  // 이미 포함됨
  ...
`);

// 재확인: 위 코드에서 typeCondition은 이미 쿼리에 포함됨
// 하지만 summary 쿼리에는 빠져 있음!
prisma.$queryRaw<RawSummary[]>(Prisma.sql`
  SELECT ...
  FROM "CommissionLedger" cl
  WHERE 1=1 ${roleCondition} ${profileCondition} ${yearMonthCondition}  // typeCondition 빠짐!
`);
```

**정정**: summary 쿼리에 typeCondition 추가
```tsx
prisma.$queryRaw<RawSummary[]>(Prisma.sql`
  SELECT ...
  FROM "CommissionLedger" cl
  WHERE 1=1 ${roleCondition} ${profileCondition} ${yearMonthCondition} ${typeCondition}  // 추가
`);
```

**예상시간**: 10분

---

#### 4. 연말정산: unsetCommissionCount 응답 누락 (P18-02)

**파일**: `src/app/api/year-end-report/route.ts:97-108, 115-124`  
**문제**: API에서 unsetCommissionCount 계산하나, grandTotal 합계에서 누락

**수정**:
```tsx
// grandTotal 계산에 unsetCommissionCount 추가
const grandTotal = agents.reduce(
  (acc, a) => ({
    totalSaleAmount:      acc.totalSaleAmount      + a.totalSaleAmount,
    totalCommission:      acc.totalCommission      + a.totalCommission,
    totalRefund:          acc.totalRefund          + a.totalRefund,
    confirmedCount:       acc.confirmedCount       + a.confirmedCount,
    unsetCommissionCount: acc.unsetCommissionCount + a.unsetCommissionCount,  // 추가
  }),
  { totalSaleAmount: 0, totalCommission: 0, totalRefund: 0, confirmedCount: 0, unsetCommissionCount: 0 }
);
```

**예상시간**: 10분

---

#### 5. 상품관리: KST 타임존 명시 (P19-04)

**파일**: `src/app/(dashboard)/products/page.tsx:67-70`  
**문제**: formatDate()에서 UTC 기준이나, 한국 출발 상품이므로 KST 기준으로 변경 필요

**수정**:
```tsx
// 기존
function formatDate(iso: string) {
  const d = new Date(iso);
  return `${d.getUTCFullYear()}-${String(d.getUTCMonth() + 1).padStart(2, "0")}-${String(d.getUTCDate()).padStart(2, "0")}`;
}

// 수정: KST(UTC+9) 기준
function formatDate(iso: string) {
  const d = new Date(iso);
  // 방법1: UTC → KST 변환
  const kst = new Date(d.getTime() + 9 * 60 * 60 * 1000);
  return `${kst.getUTCFullYear()}-${String(kst.getUTCMonth() + 1).padStart(2, "0")}-${String(kst.getUTCDate()).padStart(2, "0")}`;
  
  // 또는 toLocaleDateString 사용
  // return new Intl.DateTimeFormat('ko-KR', { year: 'numeric', month: '2-digit', day: '2-digit' }).format(d);
}
```

**예상시간**: 15분

---

#### 6-16. 나머지 P1 이슈들

**P16-03** (totalPages): API 응답에 totalPages 계산 후 포함  
**P17-04** (monthRange 페이지네이션): KEYSET pagination 검토 (선택사항)  
**P17-05, P19-05** (에러 메시지): setError() 상태 추가 후 UI에 오류 표시  
**P18-04** (refundRate 소수점): API에서 소수점 1자리로 처리, UI에서 toFixed(1) 제거  
**P18-05** (yearMonth LIKE): 범위 쿼리로 변경 (index 활용)  
**P20-01** (담당자 지정 optimistic update): assignStaff 호출 후 selectedMember 상태 즉시 업데이트  
**P20-02** (권한 검증): openDetailModal에서 역할 별 권한 검증 추가  
**P20-03** (groups 조회 지연): useEffect에서 groups 초기 로드 또는 모달 열 때 조회  
**P20-04** (그룹 생성 API 검증): 응답 필드 검증 강화 (id, name)  
**P20-05** (태그 입력 자동완성): SUGGEST_TAGS 기반 자동완성 UI 추가

**예상시간**: 60분

---

## Step 4: 실제 리펙토링 순서

1. **순차 처리** (의존성 없음, 병렬 가능):
   - P0-1: bonus null 안전성 (payslips)
   - P0-2: WITHHOLDING 부호 (commission-ledger)
   - P0-4: RefreshCw 임포트 (products)
   - P1-1 ~ P1-10: API/UI 개별 수정

2. **단일 작업** (의존성 있음):
   - P0-3: FREE_SALES TODO 구현 (DB 스키마 확인 후)

3. **검증** (모든 수정 후):
   - TypeScript 컴파일 오류 확인
   - API 응답 타입 일치성 검증
   - 권한 별 접근 제어 테스트

---

## 예상 일정

| Phase | 작업 | 시간 |
|-------|------|------|
| Step 3-1 | P0 이슈 4개 완료 | 2시간 |
| Step 3-2 | P1 이슈 16개 완료 | 4시간 |
| Step 4 | 컴파일 검증 + 테스트 | 2시간 |
| **합계** | | **8시간** |

---

**상태**: 작업지시서 작성 완료, Step 4 실행 대기

